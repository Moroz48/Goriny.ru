# Сайт-Приглашение на Свадьбу

Полнофункциональный адаптивный SPA сайт-приглашение на свадьбу с современным дизайном в стиле Pinterest.

## Особенности

- ✨ Современный минималистичный дизайн
- 📱 Полностью адаптивный для мобильных устройств
- ⏰ Таймер обратного отсчета до свадьбы
- 🗺️ Интерактивные карты Google Maps
- 📸 Галерея с lightbox просмотром
- 📝 Функциональная RSVP форма
- 🎨 Элегантные шрифты и цветовая палитра

## Структура проекта

```
saut/
├── index.html      # Основная HTML структура
├── styles.css      # Стили и адаптивный дизайн
├── script.js       # JavaScript функциональность
└── README.md       # Документация
```

## Быстрый старт

1. Откройте `index.html` в браузере или используйте локальный сервер
2. Настройте дату свадьбы в `script.js` (переменная `WEDDING_DATE`)
3. Обновите имена в `index.html`
4. Настройте Google Maps API (см. ниже)
5. Настройте RSVP форму (см. ниже)

## Настройка

### 1. Изменение даты и имен

В файле `script.js` измените:
```javascript
const WEDDING_DATE = new Date('2026-02-06T15:00:00'); // Ваша дата
```

В файле `index.html` замените:
- "Имя Невесты & Имя Жениха" на ваши имена
- "15.06.2024" на вашу дату
- Адреса и названия мест

### 2. Настройка Google Maps

1. Получите API ключ Google Maps:
   - Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
   - Создайте новый проект или выберите существующий
   - Включите "Maps JavaScript API"
   - Создайте API ключ

2. В `index.html` замените `YOUR_GOOGLE_MAPS_API_KEY` на ваш ключ:
```html
<script src="https://maps.googleapis.com/maps/api/js?key=ВАШ_API_КЛЮЧ&callback=initMaps" async defer></script>
```

3. В `script.js` обновите координаты:
```javascript
const CEREMONY_LOCATION = { lat: 55.7558, lng: 37.6173 }; // Координаты церемонии
const RECEPTION_LOCATION = { lat: 55.7558, lng: 37.6173 }; // Координаты банкета
```

### 3. Настройка RSVP формы

#### Вариант 1: Google Sheets (Рекомендуется)

1. Создайте Google Таблицу с колонками:
   - Timestamp
   - First Name
   - Last Name
   - Email
   - Guests
   - Dietary

2. Создайте Google Apps Script:
   - Откройте вашу таблицу
   - Перейдите: Расширения → Apps Script
   - Вставьте код из `google-sheets-script.js`
   - **ВАЖНО**: Замените `YOUR_SPREADSHEET_ID` на ID вашей таблицы (из URL)
   - Сохраните проект (Ctrl+S)
   - Разверните как веб-приложение:
     - Нажмите "Развернуть" → "Новое развертывание"
     - Выберите тип: "Веб-приложение"
     - Выполнение от имени: "Меня"
     - У кого есть доступ: "Все"
     - Нажмите "Развернуть"
   - Скопируйте URL веб-приложения

3. В `script.js` замените `GOOGLE_SHEETS_URL` на URL вашего скрипта:
```javascript
const GOOGLE_SHEETS_URL = 'https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec';
```

**Примечание**: Если возникают проблемы с CORS, скрипт автоматически попробует использовать GET запрос.

#### Вариант 2: Email через mailto (Простой способ)

Если вы не хотите настраивать Google Sheets, форма будет использовать mailto:

1. В `script.js` найдите строку с `recipientEmail`
2. Замените `'your-email@example.com'` на ваш реальный email:
```javascript
const recipientEmail = 'ваш-email@gmail.com';
```

#### Вариант 3: EmailJS

1. Зарегистрируйтесь на [EmailJS](https://www.emailjs.com/)
2. Создайте сервис и шаблон
3. Раскомментируйте и настройте код EmailJS в `script.js`
4. Замените `YOUR_SERVICE_ID` и `YOUR_TEMPLATE_ID`

### 4. Замена изображений

Замените URL изображений в `index.html`:
- Hero секция: `.hero-background` (строка 20)
- История: `.story-photo img` (строки 75-77)
- Галерея: `.gallery-item img` (строки 85-92)

Или используйте свои локальные изображения:
```html
<img src="images/photo1.jpg" alt="Описание">
```

## Адаптивность

Сайт полностью адаптирован для:
- 📱 Мобильных устройств (320px+)
- 📱 Планшетов (768px+)
- 💻 Десктопов (1024px+)

## Браузерная поддержка

- Chrome (последние версии)
- Firefox (последние версии)
- Safari (последние версии)
- Edge (последние версии)

## Лицензия

Свободное использование для личных проектов.

## Поддержка

При возникновении вопросов или проблем, проверьте:
1. Правильность настройки API ключей
2. Консоль браузера на наличие ошибок
3. Настройки CORS для Google Sheets интеграции

